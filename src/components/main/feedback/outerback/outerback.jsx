import Innerback from "../innerback/innerback";

const Outerback = () => {
    return(
        <section className='outerback'>
            <Innerback/>
        </section>
    )
}

export default Outerback